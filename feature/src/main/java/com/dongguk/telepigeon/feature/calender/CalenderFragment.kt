package com.dongguk.telepigeon.feature.calender

import android.os.Bundle
import android.view.View
import com.dongguk.telepigeon.feature.databinding.FragmentCalenderBinding
import com.dongguk.telpigeon.core.ui.base.BindingFragment

class CalenderFragment : BindingFragment<FragmentCalenderBinding>({ FragmentCalenderBinding.inflate(it) }) {
    override fun onViewCreated(view: View, savedInstanceState: Bundle?) {
        super.onViewCreated(view, savedInstanceState)
    }
}
